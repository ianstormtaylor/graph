
var statics = require('./statics');
var protos = require('./protos');

/**
 * Expose `plugin`.
 */

module.exports = plugin;

/**
 * Return a SQL Lite model plugin for a `database`.
 *
 * @param {Function} database
 * @return {Function}
 */

function plugin (database) {
  var db
  return function (model) {

  }
}